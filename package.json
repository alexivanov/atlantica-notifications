{
  "name": "atlantica-notifications",
  "version": "2.0.0",
  "private": true,
  "description": "Resort event reminders: Node scraper backend, PWA, and Expo native app",
  "workspaces": [
    "packages/shared",
    "packages/server",
    "packages/app"
  ],
  "scripts": {
    "build": "npm run build -w @atlantica/shared && npm run build -w @atlantica/server",
    "test": "npm run test -w @atlantica/shared && npm run test -w @atlantica/server",
    "start": "npm start -w @atlantica/server",
    "scrape": "npm run scrape -w @atlantica/server",
    "app": "npm start -w @atlantica/app"
  },
  "engines": {
    "node": ">=20"
  }
}
